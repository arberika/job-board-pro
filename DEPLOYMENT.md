# 🚀 Инструкция по деплою на GitHub и Vercel

## Шаг 1: Создание репозитория на GitHub

### Вариант A: Через веб-интерфейс

1. Зайдите на [github.com](https://github.com)
2. Нажмите кнопку **"New repository"** (зелёная кнопка справа вверху)
3. Заполните данные:
   - **Repository name**: `job-board-pro` (или любое другое название)
   - **Description**: "Professional job board platform for international employment"
   - **Visibility**: `Public` или `Private` (на ваш выбор)
   - **НЕ** ставьте галочки на "Add README" и "Add .gitignore" (они уже есть)
4. Нажмите **"Create repository"**

### Вариант B: Через GitHub CLI (если установлен)

```bash
gh repo create job-board-pro --public --source=. --remote=origin --push
```

## Шаг 2: Подключение локального репозитория к GitHub

После создания репозитория на GitHub, выполните команды:

```bash
# Перейдите в директорию проекта
cd "/Users/erika/Library/CloudStorage/GoogleDrive-admin@wowereri.com/Мой диск/САЙТЫ/МЕТОДИЧКА АСАНА/job-board-pro"

# Добавьте remote (замените YOUR_USERNAME на ваш username)
git remote add origin https://github.com/YOUR_USERNAME/job-board-pro.git

# Переименуйте ветку в main (опционально, но рекомендуется)
git branch -M main

# Запушьте код
git push -u origin main
```

**Примечание**: Если у вас настроен SSH, используйте:
```bash
git remote add origin git@github.com:YOUR_USERNAME/job-board-pro.git
```

## Шаг 3: Деплой на Vercel

### Вариант A: Через веб-интерфейс (рекомендуется)

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New"** → **"Project"**
3. **Импортируйте GitHub репозиторий**:
   - Найдите `job-board-pro` в списке
   - Нажмите **"Import"**
4. **Настройте проект** (Vercel автоматически определит Next.js):
   - **Framework Preset**: Next.js
   - **Root Directory**: `./` (оставьте по умолчанию)
   - **Build Command**: `npm run build` (автоматически)
   - **Output Directory**: `.next` (автоматически)
   - **Install Command**: `npm install` (автоматически)
5. **Environment Variables** (опционально):
   ```
   NEXT_PUBLIC_AMOCRM_URL=your_amocrm_url
   NEXT_PUBLIC_AMOCRM_FORM_ID=your_form_id
   ```
6. Нажмите **"Deploy"**

### Вариант B: Через Vercel CLI

```bash
# Установите Vercel CLI (если ещё не установлен)
npm i -g vercel

# Залогиньтесь
vercel login

# Задеплойте проект
vercel --prod
```

## Шаг 4: Проверка деплоя

После успешного деплоя Vercel предоставит вам:
- **Production URL**: `https://job-board-pro.vercel.app` (или ваш custom домен)
- **Preview URLs**: для каждого коммита в GitHub

Откройте URL в браузере и убедитесь, что сайт работает!

## 🔄 Автоматические обновления

После настройки Vercel будет автоматически:
- ✅ Деплоить каждый push в `main` ветку → Production
- ✅ Создавать preview для каждого pull request
- ✅ Оптимизировать билд для максимальной производительности

## 🌐 Подключение собственного домена

1. В Vercel Dashboard откройте ваш проект
2. Перейдите в **Settings** → **Domains**
3. Нажмите **"Add"**
4. Введите ваш домен (например, `workabroad.com`)
5. Следуйте инструкциям для настройки DNS

### Настройка DNS

Добавьте A-запись или CNAME в вашем DNS провайдере:

```
Type: A
Name: @
Value: 76.76.21.21

Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

## 📊 Мониторинг и аналитика

### Vercel Analytics

Включите Vercel Analytics в проекте:
1. Settings → Analytics → Enable

### Google Analytics

Добавьте в `src/app/layout.tsx`:

```tsx
// Добавьте в <head>
<Script
  src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"
  strategy="afterInteractive"
/>
<Script id="google-analytics" strategy="afterInteractive">
  {`
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'GA_MEASUREMENT_ID');
  `}
</Script>
```

## 🔧 Troubleshooting

### Проблема: "Permission denied" при git push

**Решение**: Настройте SSH ключ или используйте Personal Access Token:

```bash
# Создайте токен на GitHub: Settings → Developer settings → Personal access tokens
# Используйте токен вместо пароля при push
```

### Проблема: Vercel build fails

**Решение**: Проверьте логи в Vercel Dashboard и убедитесь, что:
- Все зависимости указаны в `package.json`
- Нет TypeScript ошибок (`npm run build` локально)
- Environment variables правильно настроены

### Проблема: Изображения не загружаются

**Решение**: Проверьте конфигурацию в `next.config.js`:

```javascript
images: {
  domains: ['images.unsplash.com', 'your-domain.com'],
}
```

## 📝 Следующие шаги

После деплоя рекомендуется:

1. ✅ Настроить интеграцию с amoCRM
2. ✅ Добавить Google Analytics
3. ✅ Настроить Yandex.Metrica
4. ✅ Добавить email уведомления
5. ✅ Подключить базу данных (PostgreSQL/MongoDB)
6. ✅ Реализовать админ-панель
7. ✅ Добавить личный кабинет
8. ✅ Настроить CI/CD для тестирования

## 🆘 Поддержка

Если возникли проблемы:
- 📧 Email: admin@wowereri.com
- 💬 Telegram: @your_username
- 📚 Документация: [Next.js](https://nextjs.org/docs) | [Vercel](https://vercel.com/docs)

---

**Готово!** Ваш сайт теперь доступен в интернете 🎉
